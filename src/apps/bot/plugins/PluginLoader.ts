export class PluginLoader {

    constructor() {}

    load(): void {

        // Register plugin di sini.
        // Contoh:
        // this.registry.register(new AIPlugin());
        // this.registry.register(new KasirPlugin());

    }

}